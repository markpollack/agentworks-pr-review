<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.3</version>
        <relativePath/>
    </parent>

    <groupId>io.github.markpollack</groupId>
    <artifactId>agentworks-pr-review</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <name>AgentWorks PR Review</name>
    <description>Workshop-teachable PR review pipeline using the AgentWorks stack</description>

    <properties>
        <java.version>21</java.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.github.markpollack</groupId>
                <artifactId>agent-judge-core</artifactId>
                <version>0.14.0-SNAPSHOT</version>
            </dependency>
            <!-- agentworks-bom pins junit-jupiter 5.10.3, which breaks Boot 4's MockMvcTester
                 (spring-test 7 is compiled against JUnit 6). Restore Boot's managed JUnit by
                 importing the junit-bom at Boot's version FIRST (first-declared wins). -->
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit-jupiter.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Override the workflow stack to 0.11.0-SNAPSHOT for the JudgeGate context-mapper fix
                 (agentworks-bom 1.15.0 pins 0.10.0). First-declared wins in dependencyManagement, so
                 these precede the BOM import. Remove once 0.11.0 is released and folded into the BOM. -->
            <dependency>
                <groupId>io.github.markpollack</groupId>
                <artifactId>workflow-api</artifactId>
                <version>0.11.0-SNAPSHOT</version>
            </dependency>
            <dependency>
                <groupId>io.github.markpollack</groupId>
                <artifactId>workflow-core</artifactId>
                <version>0.11.0-SNAPSHOT</version>
            </dependency>
            <dependency>
                <groupId>io.github.markpollack</groupId>
                <artifactId>workflow-flows</artifactId>
                <version>0.11.0-SNAPSHOT</version>
            </dependency>
            <dependency>
                <groupId>io.github.markpollack</groupId>
                <artifactId>workflow-journal</artifactId>
                <version>0.11.0-SNAPSHOT</version>
            </dependency>
            <dependency>
                <groupId>io.github.markpollack</groupId>
                <artifactId>workflow-spec</artifactId>
                <version>0.11.0-SNAPSHOT</version>
            </dependency>
            <dependency>
                <groupId>io.github.markpollack</groupId>
                <artifactId>agentworks-bom</artifactId>
                <version>1.15.0</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- AgentWorks: Workflow API (annotation model, AgentContext, ContextKey) -->
        <dependency>
            <groupId>io.github.markpollack</groupId>
            <artifactId>workflow-api</artifactId>
        </dependency>

        <!-- AgentWorks: Workflow DSL -->
        <dependency>
            <groupId>io.github.markpollack</groupId>
            <artifactId>workflow-flows</artifactId>
        </dependency>

        <!-- AgentWorks: workflow/v3alpha contract model + view projection (Step 1.2 serving seam).
             Ships its own Jackson 2 (com.fasterxml) — coexists with Boot 4's Jackson 3; the
             endpoints serialize v3alpha envelopes with the contract-side WireJson mapper. -->
        <dependency>
            <groupId>io.github.markpollack</groupId>
            <artifactId>workflow-spec</artifactId>
        </dependency>

        <!-- AgentWorks: Workflow Journal (typed step events into journal runs) -->
        <dependency>
            <groupId>io.github.markpollack</groupId>
            <artifactId>workflow-journal</artifactId>
        </dependency>

        <!-- AgentWorks: Journal -->
        <dependency>
            <groupId>io.github.markpollack</groupId>
            <artifactId>journal-core</artifactId>
        </dependency>

        <!-- AgentWorks: Judge framework -->
        <dependency>
            <groupId>io.github.markpollack</groupId>
            <artifactId>agent-judge-core</artifactId>
        </dependency>

        <!-- AgentWorks: Agent client facade -->
        <dependency>
            <groupId>io.github.markpollack</groupId>
            <artifactId>agent-client-core</artifactId>
        </dependency>

        <!-- AgentWorks: Claude Code bridge. Compile-visible (not runtime-only) so the
             no-Spring experiment runner can build a trace-wired AgentClient directly via
             ClaudeAgentModel.builder().workingDirectory(..).traceDir(..). The Spring app
             still resolves it the same way. -->
        <dependency>
            <groupId>io.github.markpollack</groupId>
            <artifactId>agent-claude</artifactId>
        </dependency>

        <!-- Nullness annotations -->
        <dependency>
            <groupId>org.jspecify</groupId>
            <artifactId>jspecify</artifactId>
            <version>1.0.0</version>
        </dependency>

        <!-- Spring Boot -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- WireMock -->
        <dependency>
            <groupId>org.wiremock</groupId>
            <artifactId>wiremock-standalone</artifactId>
            <version>3.13.1</version>
            <scope>test</scope>
        </dependency>

        <!-- ArchUnit -->
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>1.4.1</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>io.spring.javaformat</groupId>
                <artifactId>spring-javaformat-maven-plugin</artifactId>
                <version>0.0.47</version>
                <executions>
                    <execution>
                        <phase>validate</phase>
                        <goals>
                            <goal>validate</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.14</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.00</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <id>spring-milestones</id>
            <name>Spring Milestones</name>
            <url>https://repo.spring.io/milestone</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>

</project>
